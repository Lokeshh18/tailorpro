# Port for Express API server (defaults to 5000)
PORT=5000

# PostgreSQL Connection String (automatically provided by Render if linked)
# For local testing, customize this to your local PostgreSQL server:
DATABASE_URL=postgres://postgres:yourpassword@localhost:5432/tailorpro

# Enable PostgreSQL SSL (set to true for Render, false for local testing)
DATABASE_SSL=false
